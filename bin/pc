#!/bin/bash

export DISPLAY=:0

xd () { xdotool search --onlyvisible --class mpv key $1; }

case $1 in
    n)
        xd greater ;;
    p)
        xd p ;;
    q)
        xd q ;;
    r)
        xd less ;;
    *)
        echo -e "n: next\np: play\nq: quit\nr: prev"
        exit 1 ;;
esac
