#!/bin/bash

[ -z "$3" ] && echo "Usage: $0 name command mail" && exit 1

n=$1
c=$2
m=${@:3}

l=/tmp/$n-$(date +%s).log

exec > $l 2>&1

te () { tail -100 $l | mail -s "$HOSTNAME: $n failed" $m; exit 1; }

trap te err

date
echo "$@"

export TIMEFORMAT=%0lR

time bash -ec "$c"
