#!/bin/bash

set -e

[ -z "$1" ] && echo -e "-s: sudo\n-m: move" && exit 1

[ "$1" = -s ] && s=sudo && shift
[ "$1" = -m ] && c='mv -iv' && shift || c='cp -aiv'

d=$(date +%Y%m%d -d @$(stat -c %Y "$1"))
$s $c "$1" "${1%/}-$d"
