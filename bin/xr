#!/bin/bash

set -e

[ -z $2 ] && echo x y && exit 1

c=($(cvt $1 $2 | sed -n "s/^Modeline //p" | xargs))

m=${c[0]}

s=$(xrandr | sed -n "s/ connected primary.*//p")

set -x

xrandr --newmode $m ${c[@]:1}
xrandr --addmode $s $m

sleep 1

xrandr --output $s --mode $m
