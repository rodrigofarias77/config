#!/bin/bash

[ -z $3 ] && echo "Usage: $0 path gb mail" && exit 1

s=$(basename $0)
p=$1
g=$2
m=$3

a=$(($(stat -f -c '%a * %s / 1024**2' $p)))

[ $a -lt $(($g * 1024)) ] && printf "%'dM" $a | mail -s "$HOSTNAME: $s $p" $m

exit 0
