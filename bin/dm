#!/bin/bash

[ -z $3 ] && echo path gb mail && exit 1

s=$(basename $0)
p=$1
g=$2
m=$3

a=$(($(stat -f -c '%a * %s / 1024**2' $p)))

[ $a -lt $(($g * 1024)) ] && mail -s "$HOSTNAME: $s $p" $m <<< ${a}M

exit 0
