#!/bin/bash

s=$(basename $0)

[ -z $3 ] && echo -e "Usage: $s path x mail\n
Notify MAIL if PATH reaches XG of free space.
" && exit 1

p=$1
x=$2
m=$3

f=$(($(stat -f -c '%a * %s / 1024**3' $p)))

[ $f -le $x ] && mail -s "$HOSTNAME: $s $p" $m <<< ${f}G

exit 0
