#!/bin/bash

[ -z "$3" ] && echo "Usage: $0 name command mail" && exit 1

n=$1
c=$2
m=${@:3}

a=/tmp/$n-a.log
b=/tmp/$n-b.log

bash -c "$c" > $b

[ ! -e $a ] && cp $b $a

diff $a $b | sed -n 's/^> //p' | mail -E -s "$HOSTNAME: $n" $m

mv $b $a
